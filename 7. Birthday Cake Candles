function birthdayCakeCandles(ar) {
    let counter = 0
    let max = Math.max(...ar)
    for(let i = 0;i<ar.length;i++){
        if(ar[i] === max){
            counter++
        }
    }
    return counter
}
