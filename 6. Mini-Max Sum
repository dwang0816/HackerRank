function miniMaxSum(arr) {
    let sorted = arr.sort()
    let min = 0
    let max = 0
    for(let i = 0;i<arr.length-1;i++){
        min += arr[i]
    }
    for(let j =1;j<arr.length;j++){
        max += arr[j]
    }
    console.log(`${min} ${max}`)
}
